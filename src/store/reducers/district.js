import {data} from '../../data-district/data-district';
const initialState = data;
const districReducer = (state = initialState, { type, payload }) => {
  return state;
};

export default districReducer;

